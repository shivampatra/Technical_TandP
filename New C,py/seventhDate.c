#include<stdio.h>
#include<math.h>
#include<stdlib.h>
struct Node(){
    int data;
    struct Node *left ;
    struct Node *right;
};
int height(struct Node *root){
    if(root=NULL){
        return 0;
    }
}
int l = 1+height(root->left);
int h = 1+height(root->right);
return (1>r)?1:r;


//github.com/Anand-29